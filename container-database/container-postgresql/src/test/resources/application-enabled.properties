spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://${container.postgresql.host}:${container.postgresql.port}/${container.postgresql.database}
spring.datasource.username=${container.postgresql.username}
spring.datasource.password=${container.postgresql.password}